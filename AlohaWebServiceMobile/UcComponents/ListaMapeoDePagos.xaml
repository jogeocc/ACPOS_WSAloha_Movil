<UserControl x:Class="AlohaWebServiceMobile.UcComponents.ListaMapeoDePagos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource FondoPantallas}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="65*"/>
            <RowDefinition Height="15*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="{DynamicResource TextoMapeoPagos}" Foreground="{DynamicResource FondoLetras}" Style="{DynamicResource EstiloConfirmacionCancelar}"/>
        </Grid>

        <Grid Grid.Row="1">
            <DataGrid
                x:Name="TblMapeoPagos"
                Style="{DynamicResource Tabla}"
                ItemsSource="{Binding ModelosPagoDBF}"
                SelectedItem="{Binding Mapeo_Pagos}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        IsReadOnly="True"
                        Width="*"
                        Header="ID Pago"
                        Binding="{Binding id_pago_aloha}"/>

                    <DataGridTextColumn  
                        IsReadOnly="True"
                        Width="*"
                        Header="Nombre"
                        Binding="{Binding nombre_pago_aloha}"/>

                    <DataGridTextColumn
                        IsReadOnly="True"
                        Width="*"
                        Header="Clv Identificadora"
                        Binding="{Binding clv_mapeo_pago}"/>

                    <DataGridTemplateColumn
                        Width="*"
                        Header="Acción">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50*"/>
                                        <ColumnDefinition Width="50*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button
                                        Grid.Column="0"
                                        x:Name="BtnEliminar"
                                        Click="BtnEliminar_Click"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Foreground="{DynamicResource SCBError}"
                                        Margin="0,0,1,0"
                                        Height="28"
                                        Width="120"
                                        Content="Eliminar"/>
                                    <Button
                                        Grid.Column="1"
                                        x:Name="BtnEditar"
                                        Click="BtnEditar_Click"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Background="Transparent" 
                                        BorderBrush="Transparent"
                                        Foreground="{DynamicResource SCBError}"
                                        Margin="1,0,0,0"
                                        Height="28"
                                        Width="60"
                                        Content="Editar"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
        <Grid Grid.Row="2" Background="White" Margin="35,0,35,27">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <!--#region Botones-->
            <Grid Grid.Row="2" Grid.Column="0" >
                <Button Content="{DynamicResource TxtBtnCancelar}" Style="{DynamicResource BotonesMapeo1}" Click="BtnCancelarMapeo"></Button>
            </Grid>

            <Grid Grid.Row="2" Grid.Column="1" >
                <Button  Content="{DynamicResource TxtBtnNuevo}" Style="{DynamicResource BotonesMapeo2}" Click="BtnAgregarMapeo"></Button>
            </Grid>
            <!--#endregion-->
        </Grid>
    </Grid>
</UserControl>
