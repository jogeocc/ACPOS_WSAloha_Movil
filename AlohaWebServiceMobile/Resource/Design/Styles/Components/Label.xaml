<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style x:Key="LetrasMapeo" TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource FontZiseLetrasChicasMapeo}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontTextoLetrasChicasMapeo}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground">
            <Setter.Value>
                <SolidColorBrush Color="{DynamicResource CLRColorTablaContenidoForeground}"/>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>